version: "3.9"
services:
  expense-tracker:
    image: expense-tracker:2.0
    build: .
    container_name: expense-tracker
    environment:
      SPRING_PROFILES_ACTIVE: prod

    ports:
      - "8080:8080"
    depends_on:
      - db
  db:
    image: mysql:8.0.45-debian
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_USER: shahin
      MYSQL_DATABASE: expense-tracker
      MYSQL_PASSWORD: 1234
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
volumes:
  db-data:
